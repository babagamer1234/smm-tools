CREATE DATABASE smm;

CREATE TABLE users (
 id INT AUTO_INCREMENT PRIMARY KEY,
 name VARCHAR(100),
 email VARCHAR(100),
 password VARCHAR(100)
);

CREATE TABLE orders (
 id INT AUTO_INCREMENT PRIMARY KEY,
 service VARCHAR(100),
 link TEXT,
 quantity INT,
 status VARCHAR(50)
);
